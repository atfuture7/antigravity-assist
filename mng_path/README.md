# manage_path
Since I currently have several development environments for testing, I'm a bit too lazy to write this little tool. It can help add or remove environment directories. However, it doesn't directly write to the current session. Adding the `export` parameter will list the commands for setting the PATH.

Welcome to my channel https://www.youtube.com/@ATFutureDigital

